{
  "version": 2,
  "builds": [
    { "src": "dist/*.html", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/dist/index.html" },
    { "src": "/favicon.ico", "dest": "/dist/favicon.ico" }
  ]
}